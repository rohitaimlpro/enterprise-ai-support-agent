# Every version here was installed together and verified against this
# project's test suite (see README "Dependency notes") -- the langchain /
# langgraph / mcp ecosystem moves fast and unpinned installs can pull
# incompatible combinations (e.g. langchain-mcp-adapters needs mcp<2.0).

# --- Web framework ---
fastapi==0.140.13
starlette==1.3.1
uvicorn[standard]==0.51.0
python-multipart==0.0.32
sse-starlette==2.2.1

# --- Settings / env ---
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2
email-validator==2.3.0

# --- Database ---
sqlalchemy==2.0.51
psycopg2-binary==2.9.12

# --- Cache ---
redis==5.2.1

# --- Auth ---
python-jose[cryptography]==3.5.0
bcrypt==4.2.1

# --- Agent / RAG / MCP ---
langchain==1.3.14
langchain-core==1.5.2
langchain-text-splitters==1.1.2
langchain-google-genai==4.3.2
langgraph==1.2.10
langchain-chroma==1.1.0
chromadb==1.5.9
langchain-mcp-adapters==0.2.2
mcp==1.12.4

# --- Observability ---
prometheus-fastapi-instrumentator==8.1.0

# --- Evaluation ---
# ragas pulls in langchain-community, whose newest releases dropped the
# ChatVertexAI shim ragas.llms.base still imports unconditionally --
# eval/run_eval.py stubs that module out at runtime rather than pinning
# to an old langchain-community (which would conflict with langchain-core
# 1.x above). See the comment at the top of that file.
ragas==0.4.3
datasets==5.0.1

# --- Testing ---
pytest==8.3.4
httpx==0.28.1
